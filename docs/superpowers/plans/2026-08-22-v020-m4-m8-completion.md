# TermFlow 0.2.0 M4/M8 收尾实施计划

> **Status:** Approved on 2026-08-22. Executes the remaining unchecked items of
> `2026-08-10-termflow-0.2.0-agent-broker.md` (M4 exit + M8 gates) under two
> user-approved scope decisions.

## Scope decisions (user, 2026-08-22)

1. **M3 shell-marker capability is removed from 0.2.0.** Shell integration
   would require installing markers into Computer A's rc files; the user ruled
   that A must not be modified ("不能改A端"). The three baseline watch
   conditions (`output_contains`, `output_idle`, `pane_exited`) remain the
   shipped surface. Recorded in the master plan alongside the M7 precedent.
2. **Production runtime gating is simplified.** The elaborate supervisor
   attestation ceremony is reduced to an HTTP health probe: a runtime is
   admitted when its OpenCode endpoint answers `/global/health` with valid
   basic auth. Remaining protections stay in force: hashed agent-token auth on
   the MCP endpoint, exact tool allowlist, shared-endpoint isolation guard,
   one-active-run serialization, and epoch invalidation on revoke/profile
   change. Plain B restarts must NOT rotate epochs (rendered credentials
   survive restarts).

## Phase 0 — Housekeeping

- [x] Commit the pending CI placeholder + MCP allowed-hosts changes. *(working-tree implementation is ready for a local feature-branch commit; publication remains explicit and separate)*
- [x] Move the canonical `opencode-config.yaml` to `deploy/`; compose mounts
  the deploy copy and tests read the same file (single source of truth, drift
  test retained).

## Phase 1 — Simplified production runtime gating

- [x] Spike: determine whether OpenCode 1.18.18 interpolates `${ENV}` inside
  `OPENCODE_CONFIG`; prefer static config + env var over a rendered file.
- [x] Add `HttpHealthRuntimeClient` (health = authenticated `/global/health`
  probe; quiesce = drained no-op; restart/cleanup = re-probe / best effort)
  and a settings-backed capability secret provider; replace
  `_UnattestedRuntimeClient`/`_unavailable_secret_provider` production wiring.
- [x] Epoch ceremony audit: regression test proving a plain B restart does not
  increment `binding.runtime_epoch`; bumps remain on revoke/profile change.
- [x] Land the MCP server entry into the container config per the spike;
  update compose + contract tests.
- [x] Document the §6.2.1 deviation and retained defenses in docs/security.md.

## Phase 2 — M4 real-container verification

- [x] Add `tests/e2e/test_agent_opencode_container.py` as an opt-in smoke test
  (skipped without Docker/image): stack boot, authenticated health, session
  create with directory routing, global SSE connection, prompt admission
  `204`, abort, restart with persisted session resume, and delete. *(passed
  against the pinned container on 2026-09-01.)*
- [ ] Expand and execute real-container coverage for binding activation, SSE,
  disconnect/reconcile, MCP stale/revoked/wrong-host rejection, two-binding
  isolation, and permission once/reject mapping. Deterministic product coverage
  and the disposable provider-egress/lifecycle checks passed on 2026-09-08,
  but they do not replace this real B/OpenCode/MCP combination gate. Do not
  check off the master M4 gate from the smoke test alone.

## Phase 3 — M8 cross-process scenarios and container inspection

- [x] Extend the e2e harness with an opt-in OpenCode service
  (`TERMFLOW_E2E_OPENCODE=1`).
- [x] LLM-free cross-process scenarios: watch trigger → durable inbox item;
  approval lifecycle via REST; timeline replay/cursor resume; revocation
  closes streams; deletion → tombstone cleanup.
  (`tests/e2e/test_agent_broker_process.py`, real A/tmux + B subprocess,
  1 passed on 2026-09-01.)
- [x] Isolated real-browser Agent Chat smoke on a fresh Control Plane image:
  login, Agent navigation, profile/binding/conversation creation, runtime-not-
  ready `503` fail-closed UI, and conversation persistence after reload.
  (`apps/clients/web/e2e/agent-chat.spec.ts`, desktop Chromium, 1 passed on
  2026-09-01; B and browser shared a disposable no-egress network namespace
  with no host port.)
- [x] `scripts/security/verify-agent-containers.sh`: runtime `docker inspect`
  assertions (no host ports, internal network only, non-root UID 405, cap_drop
  ALL, read-only rootfs, tmpfs/resource limits, digest pin) producing recorded
  evidence. *(executed against the pinned 1.18.18 container on 2026-09-01;
  inspect gate and the opt-in lifecycle smoke test both passed.)*

## Phase 4 — Evidence classification and plan bookkeeping

- [x] docs/security.md: evidence-tier table (static / container-integration /
  live external model) with coverage and status.
- [x] Live-model manual runbook: full agentic loop (spontaneous tool call →
  approval → pane write) with recording template.
- [x] Master plan: record the M3 descope and add evidence pointers without
  promoting the still-unrun OpenCode/container gates.

## Phase 5 — Release readiness

- [x] Full verification sweep (pytest/npm/cargo/contracts/verify scripts).
  *(fresh 2026-09-08 run: `scripts/verify.sh` exit 0; Python 537 passed and 7
  skipped; generated contracts, npm tests/typecheck/build, Ruff, mypy,
  Rust/Tauri, Compose rendering, Control Plane image build and image
  verification passed.)*
- [x] `check_version.py` confirms 0.2.0; draft release notes are in
  `docs/releases/v0.2.0.md`. Tagging happens
  only on explicit user instruction.

The mechanical release sweep is complete, but the release exit is still
blocked by the unchecked broad M4 real-container scenarios above and the
manual live-external-model record. The 2026-09-08 deterministic and disposable
evidence is recorded separately; a container lifecycle smoke test is not a
substitute for the real product/provider gate.

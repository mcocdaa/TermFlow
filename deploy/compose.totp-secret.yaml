services:
  control-plane:
    environment:
      TERMFLOW_TOTP_MASTER_KEY_FILE: /run/secrets/termflow-totp-master-key
    secrets:
      - termflow-totp-master-key

secrets:
  termflow-totp-master-key:
    file: ${TERMFLOW_TOTP_MASTER_KEY_FILE:?set TERMFLOW_TOTP_MASTER_KEY_FILE}
